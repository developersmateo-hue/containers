# Containers Manager (Supabase + Next.js)

App web para gestionar la tabla existente `public.containers_new` en Supabase.

## Requisitos

- Node 18+
- Proyecto Supabase ya creado
- Tabla `public.containers_new` ya existente con todas las columnas y constraints

## Configuración

1. Copia `.env.example` a `.env.local`:

```bash
cp .env.example .env.local
